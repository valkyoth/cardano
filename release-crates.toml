[release]
version = "0.5.0"

[crates.cardano-valkyoth-cbor]
previous_version = "0.1.0"
version = "0.2.0"
change = "code"
publish = true
reason = "Adds explicit decode-budget limits, stateful accounting, and fail-closed resource errors."

[crates.cardano-valkyoth-primitives]
previous_version = "0.3.0"
version = "0.3.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-crypto]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-address]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-ledger]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-script]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-governance]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-node]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-rpc]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-sanitization]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-signer]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano-valkyoth-testkit]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No crate changes in the v0.5.0 decode-budget-model milestone."

[crates.cardano]
previous_version = "0.4.0"
version = "0.5.0"
change = "code"
publish = true
reason = "Re-exports the CBOR decode-budget model and publishes the v0.5.0 repository README."
